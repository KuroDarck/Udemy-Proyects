/**
 * The Principal class serves as the entry point for the movie UI application.
 */
package UI_view;

public class Principal {

    /**
     * The main method is the entry point for the application.
     * It calls the interfaceUser method from the UI_Movie class to start the user interface.
     *
     * @param args command line arguments (not used in this application)
     */
    public static void main(String[] args) {
        UI_Movie.interfaceUser();
    }
}
